name: CI

on:
  push:
    branches: [main]
    paths-ignore:
      - '**.md'
      - 'docs/**'
      - '.gitignore'
  pull_request:
    paths-ignore:
      - '**.md'
      - 'docs/**'
      - '.gitignore'

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  # Muss zur lokal geprüften pg-delta-Version passen.
  SUPABASE_CLI_VERSION: 2.111.0

jobs:
  checks:
    name: ${{ matrix.task.name }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        task:
          - name: Typecheck
            command: bun run typecheck
          - name: Linter und Formatter (Biome)
            command: bun run check
          - name: Unit-Tests
            command: bun run test
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.1

      - name: Cache Bun Dependencies
        uses: actions/cache@v4
        with:
          path: ~/.bun/install/cache
          key: ${{ runner.os }}-bun-${{ hashFiles('**/bun.lock') }}
          restore-keys: |
            ${{ runner.os }}-bun-

      - name: Dependencies installieren
        run: bun install --frozen-lockfile

      - uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x

      - name: ${{ matrix.task.name }}
        run: ${{ matrix.task.command }}

      - name: Edge-Function-Tests
        if: matrix.task.name == 'Unit-Tests'
        run: bun run test:functions

  database:
    name: DB-Schema, RLS und Integrationstests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: supabase/setup-cli@v1
        with:
          version: ${{ env.SUPABASE_CLI_VERSION }}

      - name: Cache Supabase Docker-Images
        id: cache-supabase-docker
        uses: actions/cache@v4
        with:
          path: /tmp/supabase-docker-images.tar
          key: supabase-docker-${{ env.SUPABASE_CLI_VERSION }}

      - name: Geladene Docker-Images wiederherstellen
        if: steps.cache-supabase-docker.outputs.cache-hit == 'true'
        run: docker load -i /tmp/supabase-docker-images.tar

      - name: Lokalen Supabase-Stack starten
        run: supabase start

      - name: Supabase Docker-Images fuer Cache speichern
        if: steps.cache-supabase-docker.outputs.cache-hit != 'true'
        run: |
          images=$(docker images --format '{{.Repository}}:{{.Tag}}' | grep supabase || true)
          if [ -n "$images" ]; then
            echo "Speichere Supabase Docker Images in /tmp/supabase-docker-images.tar..."
            docker save $images -o /tmp/supabase-docker-images.tar
          fi

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.1

      - name: Cache Bun Dependencies
        uses: actions/cache@v4
        with:
          path: ~/.bun/install/cache
          key: ${{ runner.os }}-bun-${{ hashFiles('**/bun.lock') }}
          restore-keys: |
            ${{ runner.os }}-bun-

      - name: Dependencies installieren
        run: bun install --frozen-lockfile

      - name: pgTAP-Suite (RLS gegen echtes Postgres, #43)
        run: bun run test:db

      - name: Integrationstests (echte lokale Instanz, #52-#58)
        run: bun run test:integration

      - name: Schema-Diff muss leer sein (Declarative-Schema-Workflow, AGENTS.md)
        run: |
          # JSON-Ausgabe wäre auch bei leerem Diff nicht leer.
          diff_output="$(supabase db diff --use-pg-delta --output-format text)"
          if [ -n "$diff_output" ]; then
            echo "$diff_output"
            echo
            echo "supabase/schemas/ und die angewendeten Migrationen weichen voneinander ab."
            echo "Neue Migration mit 'bun run db:diff -- -f <name>' erzeugen und committen."
            exit 1
          fi

      - name: Generierte DB-Typen duerfen nicht abweichen (#31)
        run: |
          bun run db:types
          if ! git diff --exit-code -- src/lib/database.types.ts; then
            echo
            echo "src/lib/database.types.ts ist veraltet. Neu generieren mit 'bun run db:types' und committen."
            exit 1
          fi

      - name: Security-Advisors (blockierend)
        run: supabase db advisors --local --type security --fail-on error

      - name: Performance-Advisors (informativ)
        run: supabase db advisors --local --type performance --fail-on none
