import type { ShoppingListWidgetProps } from './shopping-list-widget';

const ShoppingListWidget = {
  updateSnapshot(_props: ShoppingListWidgetProps): void {},
};

export default ShoppingListWidget;
