# ChainForge-Flows

Speichere hier versionierte `.cforge`-Flows aus ChainForge. Keine API-Schlüssel,
Produktivdaten oder personenbezogenen Haushaltsdaten in Flows committen.

Starte lokal aus `tools/llm-test-platform/`:

```powershell
.\scripts\chainforge.ps1 serve --host 127.0.0.1 --port 8000
```
